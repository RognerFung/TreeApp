import { AudioDirective } from './audio.directive';

describe('AudioDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioDirective();
    expect(directive).toBeTruthy();
  });
});
